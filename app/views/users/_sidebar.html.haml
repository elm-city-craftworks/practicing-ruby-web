#settings-sidebar
  = link_to "Profile", profile_settings_path,
    class: ('active' if current == 'profile')
  = link_to "Notifications", notification_settings_path,
    class: ('active' if current == 'notifications')
  = link_to "Billing", billing_settings_path,
    class: ('active' if current == 'billing')
