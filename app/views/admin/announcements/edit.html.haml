- content_for(:header) { "Edit Announcement" }

= form_for @announcement, :url => admin_announcement_path(@announcement),
  :html => { :'data-track-changes' => true } do |f|
  = render :partial => "form", :locals => {:f => f}