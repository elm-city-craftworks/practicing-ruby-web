#comments
  %hr

  - if @comments.empty?
    %p.message
      No Practicing Ruby article is complete without a good conversation. 
      Please help me out by sharing your thoughts.
  - else
    %p.message
  
    - @comments.each do |comment|
      = render :partial => "comments/show", :locals => {:comment => comment}

  = form_for @article.comments.build do |f|
    = f.hidden_field :commentable_type
    = f.hidden_field :commentable_id

    = f.text_area :body, 'data-preview' => true

    %p
      = f.submit "Comment"
