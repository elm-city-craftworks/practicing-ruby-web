#comments
  %hr

  - if @comments.empty?
    %p.message
      No 
      %i Practicing Ruby 
      article is complete without a good conversation. 
      Please share your thoughts!
  - else
    %p.message
      Now that you've read this article, what is on your mind?
    - @comments.each do |comment|
      = render :partial => "comments/show", :locals => {:comment => comment}

  = form_for @article.comments.build do |f|
    = f.hidden_field :commentable_type
    = f.hidden_field :commentable_id

    = f.text_area :body, 'data-preview' => true

    %p
      = f.submit "Comment"
